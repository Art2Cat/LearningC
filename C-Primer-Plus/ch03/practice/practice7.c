#include <stdio.h>

int main(void)
{
    float inch = 2.54;
    float height = 67.9;
    float result = inch * height;
    printf("%.2f inch equals %.2f cm.", height, result);
    int x = 13 /2;
    printf("x = %d", x);
    return 0;
}